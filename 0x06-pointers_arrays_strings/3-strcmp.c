#include "main.h"
#include <string.h>
#include <stdio.h>

/**
 * _strcmp - This is the prototype to compare strings
 *
 * @s1: This is the first string to be compared
 * @s2: This is the second string to be compared
 * Return: Always 0
 */

int _strcmp(char *s1, char *s2)
{
	return (strcmp(s1, s2));
}
